var theme = {
	// Pixel offsets for the rings
	// (subtracted from the radius)
	// General progress ring
	caOffset : 4,
	// Seconds progress ring
	csOffset : 10,
	// Minutes progress ring
	cmOffset : 16,
	// "Pie-Chart" offset
	caPieOffset : 16,

	// Ring colors
	// Foreground color
	csColor : 'rgb(41, 217, 244)',
	// Background ring color
	csBgColor : 'rgba(41, 217, 244, 0.2)',
	// Foreground color
	cmColor : 'rgb(41, 217, 244)',
	// Background ring color
	cmBgColor : 'rgba(41, 217, 244, 0.2)',
	// Foreground (active) color
	caColor : 'rgb(255, 1, 150)',
	// Background ring color
	caBgColor : 'rgba(255, 1, 150, 0.2)',
	// "Pie-Chart" color
	caPieColor : 'rgba(255, 255, 255, 0.05)'
};